# Copilot / coding-agent instructions

Read **AGENTS.md** and **AI_CHANGELOG.md** before large edits.

Production host is **ChatGPT Sites** (`mrblindbandit.net`). Never commit secrets. Prefer additive D1 migrations. Update `AI_CHANGELOG.md` after substantial AI-assisted changes.
